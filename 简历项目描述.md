# GoBlog — 项目经验

---

**项目简述：** **GoBlog** 是个游戏社区的全栈练手项目：**Go（Gin）** 做带简单页面的主站 API，含注册登录、文章评论收藏关注、话题与游戏库、**私信**、**积分与签到**、**阅读/点赞异步统计**（Redis + NSQ 批量落库）、**实时通知**（Kafka + WebSocket，离线落库）、**搜索**（Kafka 同步 Elasticsearch）、以及 **AI 对话的网关代理**；**Python（FastAPI）** 做站内 **AI 助手**，流式回复、**MCP 调博客**、**Redis / Mongo / Qdrant** 分层记忆与 Hybrid 检索、生成后的**异步反思**写长期记忆；依赖可用 **Docker Compose** 本地一键起。下表部分条目为**规划口径**，面试时按「已实现 / 设计目标」分开说即可。

---

## Go 后端

### 五条链路（叙事主线）

1. **异步统计批量落库**：阅读侧 Redis 幂等防刷 → Redis 累加阅读量并更新榜单 → NSQ 投增量；点赞侧先 MySQL 维护点赞关系，再 Redis 调计数与榜单并发 NSQ。消费者按文章 id 聚合进内存 map（加锁拷快照、解锁后批量 `UPDATE` 增量，成功后再加锁扣减 buffer；失败保留 buffer 待重试），**定时或 buffer 规模双条件** flush，降低写放大；同期排行榜为 Redis 按日分桶 ZSET、读榜时七日 `ZUNION` 合并后回源 MySQL 拼详情。
2. **积分入账（异步高吞吐）**：统一入口发分；Redis 做日上限等频控；**以 DB 流水（幂等键）为可恢复锚点**，再经 Kafka 异步入账；消费端幂等 + `balance = balance + ?` 原子加钱并记流水，**Kafka 至少一次**靠幂等防双加；定期 **流水 Σ 与余额对账** 兜底。
3. **积分商城（Redis 扛峰 + MQ 异步落库）**：**Redis 预热**先把活动/商品 **可售库存**载入缓存；兑换或抢购时由 **Lua 脚本**将 **「检查库存 → 原子扣减 → 判断是否扣成功」**打包为 **单条 `EVAL`**，保证 **全链路原子**，避免并发下先读后写竞态。Lua 成功后 **投递 MQ**（如 **RabbitMQ / Kafka / RocketMQ**），**下单受理**与前台快速返回解耦；**消费者异步**执行 **MySQL 扣余额、库存回写/校准、订单生成** 等重逻辑，可排队慢慢落库。辅以 **订单/兑换单幂等键** 与 **定期对账**，应对 **MQ 至少一次** 重投；**接受短窗 + 补偿**：**超时未落库**的预扣单 **回滚 Redis / 释放库存**。（规划：**MySQL 仍为最终账本**；与上条入账链衔接时按实际合入标注「已实现 / 规划中」。）
4. **实时通知与私信**：Kafka → 在线 WebSocket 推送；离线 Redis 红点 + `notification.store` 异步批量落库，重连按红点拉 MySQL 未读补发；私信在线直推、离线 Redis 未读累加并走同一套通知提醒（Kafka 失败时降级落库）。**（规划）连接治理**：按用户 **`sync.Map` + `time.AfterFunc` 空闲计时**，建连/客户端上行/服务端下行时重置，超时 **主动断 WS** 减僵尸连接，依赖离线链路与前端 **静默重连**。
5. **工程化与安全（横切）**：**JWT 鉴权**（REST 与 WebSocket 建连校验）；**令牌桶**全局限流；**布隆过滤器**配合空值缓存与缓存 miss 回源，降低热点读上的穿透风险；启动时 **Snowflake（`machine_id` 配置节点号）** 生成分布式友好主键，支撑多模块业务 ID 与后续水平扩展；**Docker Compose** 编排 MySQL/Redis/Kafka/NSQ 等依赖，**`profiles` 区分 dev（源码挂载 `go run`）与 app（Dockerfile 构建运行）**，环境一致、一键拉起。

---

## AI 对话助手（Python Agent）

### 五条亮点（叙事主线）

1. **多阶段编排链路（规划演进）**：**分析 Agent**（短期+中期记忆 + 工具名录摘要）→ 意图、**改写用于检索**、**工具候选**；**长期记忆检索**（接改写）；**计划 Agent**（分析结果 + 检索命中 + 候选工具的详细说明）→ **可执行计划与具体工具调用**；**执行器**按方抓药、结果回灌 **分析收口**；满足条件则 **输出**，否则 **限次** 回计划/再执行；**编排可用 LangGraph/状态机显式表达**，关键在 **每步 JSON 契约、迭代上限、模型分档**。当前仓库仍为 **「改写→路由选组→单 Agent 执行」**，迁移时注明 **已实现 / 规划中**。
2. **记忆机制**：**Redis** 短期对话原文，超量 **LLM 压缩** 为中期摘要；**MongoDB** 结构化长期事实；**Qdrant** 经验/案例向量召回；长期写入经 **判定→提取→决策**（新增/覆盖/忽略），控制重复与冲突。
3. **RAG 与检索**：对话侧 **Query 改写**（补全省略与指代）；长期记忆侧 **Qdrant 默认 hybrid**：**dense 语义 + sparse 关键词** 双路 prefetch，**服务端 RRF 融合**；**memory** 可调 **`hybrid_search`**、**`hybrid_prefetch_limit`**、**`hybrid_keyword_recall`**、可选 **`hybrid_rrf_k`**，配置项 **`hybrid_dense_weight` / `hybrid_keyword_weight`（默认 0.65 / 0.35）** 与双路语义对应，便于按业务偏置语义或关键词；可关 hybrid **回退纯 dense**；与 **MCP 工具读博客** 互补，不混为一谈。
4. **自我反思与纠正（规划异步）**：**用户可见输出完成后** 异步运行：对齐 **工具事实与答复**、**长期记忆写入/冲突合并** 等，**不阻塞主链路**；与主链路 **幂等、重试、失败可观测** 对齐。
5. **可观测性**：SSE 事件携带 **改写/路由/工具耗时** 等；**RequestID** 与 Go 网关贯通；**结构化日志**；Go 侧 **`/metrics`**（Prometheus）；Agent **`/health`**；关键路径 **分段耗时与错误分层**，便于排障与压测复盘（后续可接 OTel 等）。
