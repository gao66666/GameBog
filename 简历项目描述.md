# GoBlog — 项目经验

---

## GoBlog 博客社区（Go 后端）

**技术栈：** Go / Gin / Redis / MySQL / Kafka / Elasticsearch / WebSocket

- 基于 Gin + Handler-Service-Repository 分层架构，实现文章管理、话题讨论、游戏库、即时私信、积分系统、用户关注等完整业务模块
- 构建 Redis + MySQL 二级缓存体系，缓存 miss 自动回源回填，结合布隆过滤器拦截穿透查询；高频写场景（阅读量/点赞）走 Redis 原子操作后定时落盘
- 接入 Kafka 异步解耦通知推送、通知持久化、搜索索引同步、积分结算 4 个业务 Topic，消费者手动管理 Offset + 指数退避重试，保证消息可靠处理
- 基于 Melody 实现 WebSocket 长连接网关，支持私信实时推送、在线状态广播；连接生命周期绑定 JWT 认证，用户信息注入 Session 上下文
- 接入 Elasticsearch 全文搜索，通过 Kafka 异步消费保证 MySQL → ES 数据最终一致性
- 实现 JWT 认证中间件、令牌桶全局限流、RequestID 全链路追踪、Prometheus 接口级指标采集等基础设施

## AI 对话助手（Python Agent）

**技术栈：** Python / LangChain / FastAPI / Qdrant / MongoDB / Redis

- 设计两阶段 Agent 架构：路由阶段 LLM 仅根据工具组摘要（public/user 两组）决策选组，不加载完整工具列表；执行阶段按需加载选中组工具 + 三层记忆上下文后流式调用，避免 Token 浪费
- 构建三层渐进式记忆系统：Redis 短期记忆保留对话原文，超量后由 LLM 自动压缩为中期摘要存入 Redis；MongoDB 存储结构化事实（身份/规则/偏好），Qdrant 存储经验/案例向量，支持语义召回
- 长期记忆写入经 LLM 判定器→提取器→决策引擎三步完成：判定是否值得记、提取原子事实、对比已有记忆决策新增/覆盖/忽略，避免重复与冲突
- 封装来自后端的MCP 工具（公开：搜索/文章/话题/游戏/用户资料等；用户：个人中心/收藏/积分等），用户 Token 通过框架上下文隐式注入，对 LLM 不可见
- 接入 RAG 查询改写，用户输入先经 LLM 补全省略/指代再检索，提高命中率